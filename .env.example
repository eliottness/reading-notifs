# App
APP_URL=http://localhost:3000
NODE_ENV=development

# Database
DATABASE_URL=local.db

# Auth
BETTER_AUTH_SECRET=change-me-in-production

# Email (SMTP)
SMTP_HOST=localhost
SMTP_PORT=1025
SMTP_USER=
SMTP_PASS=
SMTP_FROM=noreply@reading-notifs.local

# Web Push (VAPID)
VAPID_PUBLIC_KEY=
VAPID_PRIVATE_KEY=
VAPID_SUBJECT=mailto:admin@reading-notifs.local
